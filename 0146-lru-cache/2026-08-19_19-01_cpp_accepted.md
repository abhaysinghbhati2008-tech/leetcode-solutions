# 146. LRU Cache
  
<br>**Problem:** https://leetcode.com/problems/lru-cache/<br>

**Difficulty:** Medium<br>
**Topics:** Hash Table, Linked List, Design, Doubly-Linked List<br>
**Language:** cpp<br>
**Status:** Accepted<br>
**Submitted:** 2026-08-19 19:01 local time

**Runtime:** 13 ms (beats 99.35759999999999%)
**Memory:** 48.1 MB (beats 98.98979999999997%)


<!-- leetgit:submissionId=2112732338 codeHash=638782024cff02206ee85967ac838ece67e126cec7339d5612e2edcaec8451a7 notesHash=e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855 -->

## Solution

```cpp
class LRUCache {
public:

    class Node{
        public:
        int key;
        int val;
        Node* next;
        Node* pre;
            Node(int k , int v){
                key = k;
                val = v;
                next = NULL;
                pre = NULL;

            }

    };
    int cap = 0;
     Node* head;
      Node* tail;
    LRUCache(int capacity) {
        cap = capacity;
        head = new Node(-1,-1);
        tail = new Node(-1,-1);

       
        head->next = tail;
        tail->pre = head;
        head->pre = NULL;
        tail->next = NULL;

    }
        void deleteNode(Node* node){
           node->next->pre = node->pre;
        node->pre->next = node->next;

        }
        void insertNode(Node* node){
            node->next = head->next;
            node->pre = head;
            head->next = node;
            node->next->pre = node;

        }
        unordered_map<int, Node*> m;

    int get(int key) {
            if(m.find(key) != m.end()){
                deleteNode(m[key]);
                insertNode(m[key]);

                return m[key]->val;

            }
            else return -1;


    }
    
    void put(int key, int value) {
            if(m.find(key) != m.end()){
                m[key]->val  = value;
                deleteNode(m[key]);
                insertNode(m[key]);

            }
            else{
                if(m.size() == cap){
                    Node* lruNode = tail->pre;
                m.erase(lruNode->key);
                deleteNode(lruNode);
                delete lruNode;

                }
              
             Node* newNode = new Node(key, value);
            insertNode(newNode);
            m[key] = newNode;

                
            }
    }
    ~LRUCache(){
         Node* curent = head;
        while(curent  != NULL){
            Node* nxt = curent->next;
            delete curent;
            curent = nxt;

        }
    }
};

/**
 * Your LRUCache object will be instantiated and called as such:
 * LRUCache* obj = new LRUCache(capacity);
 * int param_1 = obj->get(key);
 * obj->put(key,value);
 */
```
