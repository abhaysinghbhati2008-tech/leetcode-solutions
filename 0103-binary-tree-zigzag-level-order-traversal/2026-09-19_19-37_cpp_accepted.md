# 103. Binary Tree Zigzag Level Order Traversal
  
<br>**Problem:** https://leetcode.com/problems/binary-tree-zigzag-level-order-traversal/<br>

**Difficulty:** Medium<br>
**Topics:** Tree, Breadth-First Search, Binary Tree<br>
**Language:** cpp<br>
**Status:** Accepted<br>
**Submitted:** 2026-09-19 19:37 local time

**Runtime:** 0 ms (beats 100%)
**Memory:** 15.1 MB (beats 48.5321%)


<!-- leetgit:submissionId=2146711099 codeHash=8764885d8ab24e0037267379444b2fc18abe5bbd0b43c1890eb1e42bfd59e935 notesHash=e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855 -->

## Solution

```cpp
class Solution {
public:
    vector<vector<int>> zigzagLevelOrder(TreeNode* root) {
        bool f = 0;
        if(root == NULL) return {};
        vector<vector<int>> ans;
        queue<TreeNode*> q;
        q.push(root);
        while(!q.empty()){
        int size = q.size();    
        vector<int> ans1(size);
        for(int i =0 ;i<size;i++){
            int index = (!f) ? i : size-i-1;
            ans1[index] = q.front()->val;
            TreeNode* temp = q.front();
            q.pop();
            if(temp->left!=NULL) q.push(temp->left);
            if(temp->right!=NULL) q.push(temp->right);
            }
        f = !f;
        ans.push_back(ans1);
        }
    return ans;
    }
};
```
