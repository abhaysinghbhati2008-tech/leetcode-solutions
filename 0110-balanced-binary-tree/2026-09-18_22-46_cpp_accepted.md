# 110. Balanced Binary Tree
  
<br>**Problem:** https://leetcode.com/problems/balanced-binary-tree/<br>

**Difficulty:** Easy<br>
**Topics:** Tree, Depth-First Search, Binary Tree<br>
**Language:** cpp<br>
**Status:** Accepted<br>
**Submitted:** 2026-09-18 22:46 local time

**Runtime:** 0 ms (beats 100%)
**Memory:** 23 MB (beats 54.56370000000001%)


<!-- leetgit:submissionId=2145976621 codeHash=19ea717265ad256c78f314b8f8feb00b342fbc3293daec153e5ed91e3ff634f8 notesHash=e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855 -->

## Solution

```cpp
class Solution {
public:
    int height(TreeNode* root){
        if(root == NULL) return 0;
        int l = height(root->left);
        int r= height(root->right);
        return 1+max(l,r);
    }
    bool isBalanced(TreeNode* root) {
        // vector<int> ans;
        // int count = 0;
        // queue<TreeNode* >q;
        // q.push(root); count++;
        // ans.push_back(root->val);
        // while(!q.empty()){
        //     int size = q.size();
        //     count++;
        //     for(int i =0 ;i<size;i++){
        //         if(q.front()->left!=NULL) q.push(q.front()->left);
        //         if(q.front()->right!=NULL) q.push(q.front()->right);
        //         ans.push_back(q.front()->val);
        //         q.pop();
        //     }
        // }
        // return 
        if(root == NULL) return true;
        int l = height(root->left);
        int r = height(root->right);
        if(abs(l-r) > 1) return false;
        bool left = isBalanced(root->left);
        bool right = isBalanced(root->right);
        if(!left || !right) return false;
        return true;
    }
};
```
